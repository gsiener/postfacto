import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  // Base controller for shared functionality
  // Extend this controller for common behavior across your Stimulus controllers
}
